# Running on Replit

This project is a React and TypeScript site built with Vite.

- Start the development server with the **Start application** workflow.
- The workflow runs `npm run dev` and serves the site on port 5000.
- Run `npm run build` to create a production build.
- Run `npm run lint` to type-check the project.